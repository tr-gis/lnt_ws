This package has the description of robot. For the URDF file look into robots folder. It also has the collada file later used when 
trying to create the IKfast plugin for the robot.

NOTE: the robot is gray in color and hence easy to miss if screen brightness is set low.
